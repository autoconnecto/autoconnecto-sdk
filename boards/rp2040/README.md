# RP2040 (Pico) — Autoconnecto quick start (docs)

RP2040 devices are common in prototypes. For cloud connectivity you typically add:

- Wi‑Fi module (e.g. ESP‑AT) or Ethernet module
- TLS + MQTT client

