// Convenience include for Arduino Library format 1.5:
// only `src/` is on the include path, not subfolders.
// Implementation lives in src/ota/OtaUpdate.{h,cpp}.
#pragma once
#include "ota/OtaUpdate.h"
